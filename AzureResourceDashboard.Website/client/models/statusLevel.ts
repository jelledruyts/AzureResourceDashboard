module app.models {
    "use strict";

    export enum StatusLevel {
        None = 0,
        Info = 1,
        Success = 2,
        Warning = 3,
        Error = 4,
        Active = 5,
        Inactive = 6
    }
}